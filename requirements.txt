# =======================================================================
# Dynamic Pricing — POC dependencies
# Tested with Python 3.11/3.12. (pmdarima chưa ổn định trên Python 3.13.)
# Cài: pip install -r requirements.txt
# =======================================================================

# ---- Core data ----
pandas>=2.1,<3.0
numpy>=1.24,<2.0          # pmdarima 2.x vẫn pin numpy <2
pyarrow>=14.0             # cho parquet feature store
python-dateutil>=2.8

# ---- Time series (Forecast model) ----
statsmodels>=0.14
pmdarima>=2.0.4           # auto_arima

# ---- ML (Demand model) ----
scikit-learn>=1.3
lightgbm>=4.1

# ---- Calendar ----
holidays>=0.40            # locale 'VN' đã hỗ trợ Tết, 30/4, 2/9

# ---- App + Viz ----
streamlit>=1.30
plotly>=5.18
matplotlib>=3.7         # EDA notebooks (Plotly cho app, Matplotlib cho notebook)

# ---- Persistence ----
joblib>=1.3

# ---- Notebook / dev ----
jupyter>=1.0
ipykernel>=6.27

# ---- Tests ----
pytest>=7.4
