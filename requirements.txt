# =======================================================================
# Dynamic Pricing — POC dependencies (PINNED — versions đã verify ở Bước 7)
# Python 3.11/3.12 (pmdarima chưa stable trên 3.13+).
# macOS: cần `brew install libomp` cho LightGBM.
# Cài: pip install -r requirements.txt
# =======================================================================

# ---- Core data ----
pandas==2.3.3
numpy==1.26.4              # pmdarima 2.x vẫn pin numpy <2
pyarrow==24.0.0            # cho parquet feature store
python-dateutil==2.9.0.post0

# ---- Time series (Forecast model) ----
statsmodels==0.14.6
pmdarima==2.1.1            # auto_arima
prophet==1.3.0             # alternative forecast (Bước 8 extra)

# ---- ML (Demand model) ----
scikit-learn==1.8.0
lightgbm==4.6.0

# ---- Calendar ----
holidays==0.96             # locale 'VN' hỗ trợ Tết, 30/4, 2/9

# ---- App + Viz ----
streamlit==1.57.0
plotly==6.7.0
matplotlib==3.10.9         # EDA notebooks

# ---- Persistence ----
joblib==1.5.3

# ---- Notebook / dev ----
jupyter==1.1.1
ipykernel==7.2.0

# ---- Tests ----
pytest==9.0.3
